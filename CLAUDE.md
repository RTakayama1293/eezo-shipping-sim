# CLAUDE.md

## プロジェクト概要
- **目的**: EEZO（北海道食材EC）のShopify移行に伴う送料・ディスカウントロジックの設計と検証
- **手法**: 3層構造（商品価格転嫁＋固定送料＋合わせ買い割引）のシミュレーション
- **成功基準**: 全購買シナリオで粗利率15%以上を確保しつつ、顧客体験として「送料明朗・まとめ買いお得」を実現

---

## Core Philosophy（基本原則）

### 1. Plan Before Execute（計画先行）
送料ロジックは一度決めると顧客体験に直結する。いきなり実装せず、パラメータを変えながらシミュレーションで検証する。

### 2. Scenario-Driven（シナリオ駆動）
抽象的な最適化ではなく、具体的な購買シナリオ（カート構成）を列挙し、各ケースで粗利・顧客体験を検証する。

### 3. Immutability（不変性）
data/raw/ のデータは絶対に変更しない。パラメータ変更はすべてconfigで管理。

### 4. Visual Output（可視化重視）
シミュレーション結果は必ずグラフ＋テーブルで出力。意思決定者（蜂巣さん）への説明に使える形にする。

---

## Critical Rules（絶対ルール）

- data/raw/ 配下のファイルを編集しない
- シミュレーションのパラメータはすべて data/raw/config.json で管理
- 実験結果は experiments/ 配下に独立保存（上書き禁止）
- 金額計算は必ず整数（円）で行い、浮動小数点の丸め誤差を避ける
- 粗利率の計算式: `粗利率 = (販売価格合計 - 仕入原価合計 - 実配送コスト) / 販売価格合計`

---

## ビジネスコンテキスト

### 3層構造モデル

**第1層：商品価格への送料転嫁**
- 仕入原価に粗利マージン＋送料補填分を乗せて販売価格を設定
- 市場価格と比較して「若干高いけどそんなものか」の範囲に収める

**第2層：固定送料の表示**
- 全国一律1,000円（顧客表示額）
- 実際の配送コストとの差分は第1層で吸収

**第3層：合わせ買いディスカウント**
- 同一仕入先から複数SKU購入時、同梱で実配送コスト増なし
- 各商品に乗っている送料補填分の「浮き」を割引として還元
- Shopifyの自動ディスカウント機能で実装

### 前提パラメータ

| パラメータ | 値 | 備考 |
|-----------|-----|------|
| 主力商品価格帯 | 5,000〜10,000円 | BtoC販売価格 |
| 配送コスト基準 | 1,510円 | 5kg冷凍便・関西（1,290+220） |
| 固定送料（顧客表示） | 1,000円 | 全国一律 |
| 最低粗利率 | 15% | 全シナリオで確保必須 |
| 主要仕入先 | あぷかの森（エゾシカ） | 当面のフォーカス |
| 最大仕入先数/1注文 | 3社 | これ以上は実質発生しない想定 |

### Shopify制約

- 自動ディスカウント: 1注文につき1つのみ適用可能
- Shipping Profile: 地域別・重量別の送料設定が可能
- Shopify Functions（カスタム送料ロジック）: Plus必須（月額$2,300）→ 不採用
- ディスカウントコード: 手動入力型、自動ディスカウントと併用不可

---

## データセット情報

### 商品マスタ (data/raw/products.csv)
| カラム名 | 型 | 説明 | 備考 |
|----------|-----|------|------|
| sku | str | 商品コード | PRD_SNJ_xxx |
| name | str | 商品名 | |
| supplier | str | 仕入先名 | あぷかの森、等 |
| cost_price | int | 仕入原価（円） | 税抜 |
| weight_g | int | 重量（g） | 配送サイズ判定用 |
| temp_zone | str | 温度帯 | frozen/chilled/ambient |
| category | str | カテゴリ | エゾシカ/水産/農産/加工品 |

### 配送料金テーブル (data/raw/shipping_rates.csv)
| カラム名 | 型 | 説明 | 備考 |
|----------|-----|------|------|
| region | str | 配送先地域 | hokkaido/tohoku/kanto/... |
| size_code | str | サイズ区分 | 60/80/100/120 |
| base_rate | int | 基本配送料（円） | |
| cool_surcharge | int | クール便加算（円） | 220円 |

### シミュレーション設定 (data/raw/config.json)
```json
{
  "fixed_shipping_display": 1000,
  "min_margin_rate": 0.15,
  "markup_strategy": {
    "shipping_buffer_per_item": 500,
    "margin_rate": 0.20
  },
  "discount_tiers": [
    {"min_items": 2, "same_supplier": true, "discount": 300},
    {"min_items": 3, "same_supplier": true, "discount": 700},
    {"min_items": 4, "same_supplier": true, "discount": 1200}
  ],
  "scenarios": []
}
```

---

## Available Commands（利用可能なコマンド）

| キーワード | 内容 | 使い方 |
|-----------|------|--------|
| `シミュレーション実行` | 全シナリオの損益シミュレーション | 「全シナリオでシミュレーション実行して」 |
| `感度分析` | パラメータの感度分析 | 「転嫁額の感度分析をして」 |
| `シナリオ追加` | 購買シナリオの追加 | 「高単価3品のシナリオ追加して」 |
| `レポート生成` | 結果をMarkdown+グラフで出力 | 「レポート生成して」 |
| `境界ケース` | 最悪/最良ケースの特定 | 「境界ケースを分析して」 |

---

## Context Modes（コンテキストモード）

### シミュレーションモード（デフォルト）
- パラメータを変えながら損益を検証
- グラフ＋テーブルで結果を可視化
- 「これでいける」の判断材料を作る

### 実装モード
- シミュレーション結果が確定した後
- Shopifyの設定手順書を生成
- ディスカウントの具体的な設定値を出力

---

## 技術スタック

- Python 3.x
- pandas, numpy（データ処理）
- matplotlib, seaborn, japanize-matplotlib（可視化）
- streamlit（インタラクティブダッシュボード、オプション）

---

## ディレクトリルール

1. **data/raw/** - 商品マスタ・配送料金・設定ファイル。**絶対に編集しない**
2. **data/processed/** - シミュレーション用に加工したデータ
3. **experiments/expXXX_[説明]/** - 各シミュレーション実験は独立ディレクトリ
4. **src/** - シミュレーションエンジン（共通コード）
5. **outputs/** - 最終成果物（レポート、設定値一覧）

---

## コーディング規約

- 型ヒント必須
- docstring必須（Google形式）
- 金額は int 型（円単位、浮動小数点禁止）
- 率（粗利率等）は Decimal または float で計算し、表示時に % 変換
- f-string優先
